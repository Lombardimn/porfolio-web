---
interface Props {
	href: string;
	target?: string;
	rel?: string;
	class?: string;
}

const { href, target, rel } = Astro.props;
---

<a 
	href={href} 
	target={target} 
	rel={rel}
	class="relative flex py-2 px-8 rounded-full overflow-hidden shadow-tl-shadow-md text-center text-tl-text-over no-underline leading-tight whitespace-nowrap place-content-center gap-3 transition ease-in-out duration-200 hover:outline hover:outline-2 hover:outline-tl-overlay focus:outline focus:outline-2 focus:outline-tl-overlay after:content-[''] after:absolute after:inset-0 after:pointer-events-none hover:scale-105 focus:scale-105 text-large md:py-3 md:px-10 md:text-xlarge bg-gradient-to-r from-[#078C5B] via-tl-regular to-tl-dark dark:from-[#ca7879] dark:via-td-regular dark:to-td-light dark:text-td-grey-0"
>
	<slot />
</a>