---
interface Props {
  variant?: 'offset' | 'small';
}

const { variant } = Astro.props as Props;
---

<ul class:list={[
  'grid auto-rows-fr gap-4 list-none p-0 last:odd:col-span-2',
  { offset: variant === 'offset', small: variant === 'small' }
]}>
  <slot />
</ul>